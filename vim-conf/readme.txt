Linux Vimrc Configuration

Steps: 

run the conf_vim.sh as root; (chmod +x conf_vim.sh)
then it is ok.


#old note:
#Step1:
#cp vimrc /etc/  #this action will overwrite original vimrc file.
#
#Step2:
#yum install -y ctags cscope
#mv 4 directories into /usr/share/vim/vimfile/
#vim plugin/supertab and implemented :so%
